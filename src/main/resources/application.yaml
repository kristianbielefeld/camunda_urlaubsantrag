spring:
  application:
    name: camunda_urlaubsantrag

server:
  port: 8081

camunda:
  client:
    mode: self-managed
    grpc-address: http://localhost:26500
    rest-address: http://localhost:8080
    auth:
      method: none
    # --- Falls dein Cluster Basic Auth verlangt (Operate-Login mit demo/demo): ---
    # auth:
    #   method: basic
    #   username: demo
    #   password: demo

logging:
  level:
    io.camunda: INFO